version: '3.3'
services:
  bitcoin-abc-testnet:
    build:
      context: ./
      dockerfile: Dockerfile
    command: bitcoind -testnet -txindex=0 -listen=1 -server=1 -irc=1 -upnp=1 -port=20008 -rpcport=20009 -rpcuser=username -rpcpassword=password -printtoconsole=1
    logging:
      driver: "json-file"
      options:
        max-size: "100k"
        max-file: "10"
    ports:
      - 20009:20009
      - 20008:20008
    restart: unless-stopped
    volumes:
      - bitcoin-abc-testnet:/home/bitcoin/.bitcoin

volumes:
  bitcoin-abc-testnet:
